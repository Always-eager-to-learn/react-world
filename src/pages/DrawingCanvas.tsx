import MainCanvas from "../components/CanvasDrawing/MainCanvas"

const DrawingCanvas = () => {
  return (
    <section className="h-dvh flex flex-col gap-0.5">
      <MainCanvas />
    </section>
  )
}

export default DrawingCanvas
